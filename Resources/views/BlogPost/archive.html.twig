<ul class="unstyled">
	{% if months|length > 0 %}
		{% for month, ps in months %}
			<li> 
				<i class="icon-calendar"></i>
				<a href="#">{{ month }} ({{ ps }})</a>
			</li>
		{% endfor %}
	{% else %}
		<li>No months.</li>
	{% endif %}
</ul>