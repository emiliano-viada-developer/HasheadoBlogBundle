{% for key, flash in app.session.getFlashes() %}
	<div class="alert alert-{{ key }}">
		<button type="button" class="close" data-dismiss="alert">×</button>
		<strong>{{ key|capitalize }}</strong>: {{ flash }}
	</div>
{% endfor %}